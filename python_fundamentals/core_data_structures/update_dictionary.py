#!/usr/bin/env python3
"""Update or add an item in a dictionary."""


def update_dictionary(a_dictionary, key, value):
    """Update key with value and return the dictionary."""
    a_dictionary[key] = value
    return a_dictionary
